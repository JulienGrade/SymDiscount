{% extends 'base.html.twig' %}

{% block title %}Inscritpion{% endblock %}

{% block body %}

<div class="container">
    <h1>Créez votre espace membre</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Confirmez l'inscription</button>
    {{ form_end(form) }}
</div>

{% endblock %}